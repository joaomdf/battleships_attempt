from lib.ship import Ship

class ShipPlacement:
    def __init__(self, length, row, column, orientation):
        self.length = length
        self.row = row
        self.column = column
        self.orientation = orientation
    